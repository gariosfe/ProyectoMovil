name: proyectomovil
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  badges: ^3.1.2
  cupertino_icons: ^1.0.8
  material_design_icons_flutter: ^7.0.7296  # Agregado el paquete material_design_icons_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
